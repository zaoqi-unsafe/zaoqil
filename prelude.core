(record
 module record
 id (λ x x)
 or (λ x (λ y (if x t y)))
 and (λ x (λ y (if x (if y t f) f)))
 list? (λ xs (or (null? xs) (list? (cdr xs))))
 map (λ f (λ xs
            (if (null? xs)
                ()
                (cons (f (car xs)) (map f (cdr xs))))))
 )
